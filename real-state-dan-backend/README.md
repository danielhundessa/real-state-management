# Backend

## Local

```shell
# if necessary
docker rm $(docker ps --filter status=exited -q)

docker-compose up -d
```

## API Documentation

- Swagger: `http://localhost:8080/swagger-ui/index.html`
